{
  "name": "devISE",
  "version": "0.1.0",
  "description": "devISE orchestrator for managed developer/debugger project loops",
  "type": "module",
  "bin": {
    "devISE": "./dist/src/cli.js"
  },
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "blessed": "^0.1.81",
    "yaml": "^2.8.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.27",
    "@types/node": "^24.6.0",
    "typescript": "^5.9.2"
  }
}
